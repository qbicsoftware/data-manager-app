<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>life.qbic.datamanager</groupId>
    <artifactId>datamanager</artifactId>
    <version>1.15.0</version>
  </parent>

  <artifactId>openbis-api-clean</artifactId>
  <name>OpenBIS API (de-collided)</name>
  <description>
    Repackaging of the upstream life.qbic:openbis-api shaded uber-jar that relocates the bundled
    org.springframework classes into a private namespace.

    Rationale: the upstream artifact is a shaded uber-jar that bundles an old (2017-era)
    Spring Framework compiled against javax.servlet. It defines
    org.springframework.web.WebApplicationInitializer with onStartup(javax.servlet.ServletContext),
    which collides with the real spring-web (Spring Framework 7 / Jakarta) version that uses
    onStartup(jakarta.servlet.ServletContext). Depending on classpath ordering, javac picks the
    wrong one and compilation of Application.java fails.

    Removing the bundled spring would break openbis at runtime, because real Spring 7 no longer
    ships org.springframework.remoting.* that the openbis client (ch.systemsx.cisd.common.spring,
    com.marathon.util.spring) depends on. Relocating it keeps openbis exactly as-is while removing
    it from the shared org.springframework namespace so the real Spring is authoritative at both
    compile time and runtime.

    The relocation is applied during the compile phase and the result is unpacked into this
    module's target/classes so that other modules of the same reactor build see the relocated
    classes (a pure shade-repackaging module would only expose them via its jar at package time,
    which reactor consumers never see).
  </description>

  <dependencies>
    <!--
      Input of the relocation only. It is marked optional and removed from the reduced
      published POM, so consumers never see the upstream shaded jar again - they receive just the
      relocated openbis classes and rely on the real Spring/Jackson/etc. artifacts for third-party
      types that live outside the relocated space.
    -->
    <dependency>
      <groupId>life.qbic</groupId>
      <artifactId>openbis-api</artifactId>
      <version>20.10.7.3</version>
      <classifier>r1700646105</classifier>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        Make the module's (initially empty) main artifact available early so maven-shade-plugin
        can run during the compile phase - this is required so that 'mvn compile' puts the
        relocated classes into this module's target/classes for same-reactor consumers.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>default-jar-early</id>
            <phase>compile</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!--
        Relocate the bundled org.springframework into a private namespace, reading from the early
        jar + the openbis dependency. Runs during the compile phase so 'mvn compile' and other
        modules in the same reactor see the relocated classes in target/classes.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <configuration>
          <createDependencyReducedPom>true</createDependencyReducedPom>
          <relocations>
            <relocation>
              <pattern>org.springframework</pattern>
              <shadedPattern>life.qbic.openbis.shaded.org.springframework</shadedPattern>
            </relocation>
          </relocations>
          <transformers>
            <transformer
              implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
          </transformers>
        </configuration>
        <executions>
          <execution>
            <id>relocate</id>
            <phase>compile</phase>
            <goals>
              <goal>shade</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Unpack the relocated jar into target/classes so same-reactor modules find the classes. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>unpack-relocated</id>
            <phase>compile</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <unzip src="${project.build.directory}/${project.build.finalName}.jar"
                  dest="${project.build.outputDirectory}"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
/**
 * User groups bounded context — cross-context API facades.
 *
 * <p>Shared API interfaces and self-contained data transfer types consumed by other bounded
 * contexts (e.g. project-management authorization) and the application (datamanager-app).
 *
 * @since 1.19.0
 */
package life.qbic.usergroups.api;